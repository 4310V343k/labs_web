NODE_ENV=development
FRONTEND_LOCALHOST_PORT=5173
BACKEND_LOCALHOST_PORT=5000

# PostgreSQL
DB_PORT=5432

# CORS - must match the origin the browser sends.
FRONTEND_URL=http://localhost:5173

# Vite proxy target in dev - override if running backend non-locally
VITE_API_URL=http://backend:5000
